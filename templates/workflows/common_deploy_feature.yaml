name: Deploy Preview

on:
  pull_request:
    branches: [main]
  push:
    branches-ignore:
      - main

env:
  NODE_VERSION: 22
  CF_PROJECT_NAME: __CF_PROJECT__
  CF_DIST_PATH: __CF_DIST_PATH__

jobs:
  preview:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
      - uses: pnpm/action-setup@v3
        with:
          version: 9.12.0
          run_install: false
      - name: Install dependencies
        run: |
          corepack enable
          pnpm install --frozen-lockfile
      - name: Build app
        run: __NX_BUILD_CMD__
      - name: Deploy preview to Cloudflare Pages
        run: pnpm dlx wrangler pages deploy "$CF_DIST_PATH" --project-name "$CF_PROJECT_NAME" --branch "${{ github.head_ref || github.ref_name }}" --commit-dirty true
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
